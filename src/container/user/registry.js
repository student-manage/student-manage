import React, { Component } from 'react'

class Registry extends Component {
    state = {}
    render() {
        return (
            <div>registry</div>
        );
    }
}

export default (Registry);